diff --git a/node_modules/svelte-lightweight-charts/.bun-tag-c5257ae9dfdaa3a0 b/.bun-tag-c5257ae9dfdaa3a0
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/internal/series.js b/internal/series.js
index 4b59916ebcb99e1d1519d159d744a5b57e79602f..1591b2b1f54bf39255df1bd75df48ec8849088dc 100644
--- a/internal/series.js
+++ b/internal/series.js
@@ -1,3 +1,5 @@
+import { AreaSeries, BarSeries, CandlestickSeries, HistogramSeries, LineSeries, BaselineSeries, createSeriesMarkers } from 'lightweight-charts';
+
 export function series(target, params) {
     let subject = createSeries(target, params);
     let reference;
@@ -43,39 +45,39 @@ export function series(target, params) {
 function createSeries(chart, params) {
     switch (params.type) {
         case 'Area': {
-            const series = chart.addAreaSeries(params.options);
+            const series = chart.addSeries(AreaSeries, params.options);
             series.setData(params.data);
-            series.setMarkers(params.markers);
+            createSeriesMarkers(series, params.markers);
             return series;
         }
         case 'Bar': {
-            const series = chart.addBarSeries(params.options);
+            const series = chart.addSeries(BarSeries, params.options);
             series.setData(params.data);
-            series.setMarkers(params.markers);
+            createSeriesMarkers(series, params.markers);
             return series;
         }
         case 'Candlestick': {
-            const series = chart.addCandlestickSeries(params.options);
+            const series = chart.addSeries(CandlestickSeries, params.options);
             series.setData(params.data);
-            series.setMarkers(params.markers);
+            createSeriesMarkers(series, params.markers);
             return series;
         }
         case 'Histogram': {
-            const series = chart.addHistogramSeries(params.options);
+            const series = chart.addSeries(HistogramSeries, params.options);
             series.setData(params.data);
-            series.setMarkers(params.markers);
+            createSeriesMarkers(series, params.markers);
             return series;
         }
         case 'Line': {
-            const series = chart.addLineSeries(params.options);
+            const series = chart.addSeries(LineSeries, params.options);
             series.setData(params.data);
-            series.setMarkers(params.markers);
+            createSeriesMarkers(series, params.markers);
             return series;
         }
         case 'Baseline': {
-            const series = chart.addBaselineSeries(params.options);
+            const series = chart.addSeries(BaselineSeries, params.options);
             series.setData(params.data);
-            series.setMarkers(params.markers);
+            createSeriesMarkers(series, params.markers);
             return series;
         }
     }
